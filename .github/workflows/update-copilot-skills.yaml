name: 🔄 Update Copilot Skills

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: devantler-tech/reusable-workflows/.github/workflows/update-copilot-skills.yaml@11f70d3be475887db4f262f415fcb19c72a1fa73 # v3.0.1
    with:
      dir: plugins
