name: 🔄 Update Copilot Skills

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

permissions: {}

jobs:
  update:
    uses: devantler-tech/reusable-workflows/.github/workflows/update-copilot-skills.yaml@main
    with:
      dir: plugins
